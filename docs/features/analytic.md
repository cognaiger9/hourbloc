### cached pattern for analytic: overview, day, month, year
There are cases for invalidate the cache
- User finish the timer (create new block) -> invalidate all
- User manually create block or delete or update -> invalidate all